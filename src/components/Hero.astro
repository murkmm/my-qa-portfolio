---
interface Props {
	title: string;
	tagline?: string;
	/** Optional second line under the tagline, for supporting detail. */
	subtagline?: string;
	align?: 'start' | 'center';
}

const { align = 'center', tagline, subtagline, title } = Astro.props;
---

<div class:list={['hero stack gap-4', align]}>
	<div class="stack gap-2">
		<h1 class="title">{title}</h1>
		{tagline && <p class="tagline">{tagline}</p>}
		{subtagline && <p class="subtagline">{subtagline}</p>}
	</div>
	<slot />
</div>

<style>
	.hero {
		font-size: var(--text-lg);
		text-align: center;
	}

	.title,
	.tagline,
	.subtagline {
		max-width: 37ch;
		margin-inline: auto;
	}

	.subtagline {
		color: var(--gray-400);
		font-size: var(--text-md);
	}

	.title {
		font-size: var(--text-3xl);
		color: var(--gray-0);
	}

	@media (min-width: 50em) {
		.hero {
			font-size: var(--text-xl);
		}

		.start {
			text-align: start;
		}

		.start .title,
		.start .tagline,
		.start .subtagline {
			margin-inline: unset;
		}

		.title {
			font-size: var(--text-5xl);
		}
	}
</style>
